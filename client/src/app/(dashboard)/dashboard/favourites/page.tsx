import HeroText from "@/app/features/common/components/heroText";
import DashboardLayout from "../layout";

export default function Favourites() {
  {
    return (
      <main>
        <HeroText content="Favourites" />
      </main>
    );
  }
}
